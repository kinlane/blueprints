name: Deploy to Lambda
on: [push]
jobs:
  deploy_source:
    name: Deploy to Lambda
    strategy:
      matrix:
        node-version: [12.x]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: npm install and build
        run: |
          npm ci
          npm run build --if-present
        env:
          CI: true
          
      # Blueprints
      - name: blueprints get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r blueprints-get.zip blueprints/get/index.js node_modules
      - name: blueprints get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: blueprints-staging-blueprints-get
          zip_file: blueprints-get.zip

      - name: blueprints post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r blueprints-post.zip blueprints/post/index.js node_modules
      - name: blueprints post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: blueprints-staging-blueprints-post
          zip_file: blueprints-post.zip

      - name: blueprints id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r blueprints-b_id-get.zip blueprints/b_id/get/index.js node_modules
      - name: blueprints id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: blueprints-staging-blueprints-blueprint_id-get
          zip_file: blueprints-b_id-get.zip       

      - name: blueprints id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r blueprints-b_id-put.zip blueprints/b_id/put/index.js node_modules
      - name: blueprints id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: blueprints-staging-blueprints-blueprint_id-put
          zip_file: blueprints-b_id-put.zip       

      - name: blueprints id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r blueprints-b_id-delete.zip blueprints/b_id/delete/index.js node_modules
      - name: blueprints id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: blueprints-staging-blueprints-blueprint_id-delete
          zip_file: blueprints-b_id-delete.zip                              

      - name: blueprints id tags get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r blueprints-b_id-tags-get.zip blueprints/b_id/tags/get/index.js node_modules
      - name: blueprints id tags get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: blueprints-s-blueprints-b_id-tags-get
          zip_file: blueprints-b_id-tags-get.zip

      - name: blueprints id tags post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r blueprints-b_id-tags-post.zip blueprints/b_id/tags/post/index.js node_modules
      - name: blueprints id tags post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: blueprints-s-blueprints-b_id-tags-post
          zip_file: blueprints-b_id-tags-post.zip



      # actions
      - name: actions get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r actions-get.zip actions/get/index.js node_modules
      - name: actions get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: actions-s-actions-get
          zip_file: actions-get.zip

      - name: actions post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r actions-post.zip actions/post/index.js node_modules
      - name: actions post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: actions-s-actions-post
          zip_file: actions-post.zip

      - name: actions id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r actions-a_id-get.zip actions/a_id/get/index.js node_modules
      - name: actions id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: actions-s-actions-a_id-get
          zip_file: actions-a_id-get.zip       

      - name: actions id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r actions-a_id-put.zip actions/a_id/put/index.js node_modules
      - name: actions id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: actions-s-actions-a_id-put
          zip_file: actions-a_id-put.zip       

      - name: actions id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r actions-a_id-delete.zip actions/a_id/delete/index.js node_modules
      - name: actions id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: actions-s-actions-a_id-delete
          zip_file: actions-a_id-delete.zip                              

      # answers
      - name: answers get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r answers-get.zip answers/get/index.js node_modules
      - name: answers get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: answers-s-answers-get
          zip_file: answers-get.zip

      - name: answers post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r answers-post.zip answers/post/index.js node_modules
      - name: answers post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: answers-s-answers-post
          zip_file: answers-post.zip

      - name: answers id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r answers-a_id-get.zip answers/a_id/get/index.js node_modules
      - name: answers id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: answers-s-answers-a_id-get
          zip_file: answers-a_id-get.zip       

      - name: answers id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r answers-a_id-put.zip answers/a_id/put/index.js node_modules
      - name: answers id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: answers-s-answers-a_id-put
          zip_file: answers-a_id-put.zip       

      - name: answers id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r answers-a_id-delete.zip answers/a_id/delete/index.js node_modules
      - name: answers id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: answers-s-answers-a_id-delete
          zip_file: answers-a_id-delete.zip                              

      # elements
      - name: elements get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r elements-get.zip elements/get/index.js node_modules
      - name: elements get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: elements-s-elements-get
          zip_file: elements-get.zip

      - name: elements post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r elements-post.zip elements/post/index.js node_modules
      - name: elements post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: elements-s-elements-post
          zip_file: elements-post.zip

      - name: elements id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r elements-e_id-get.zip elements/e_id/get/index.js node_modules
      - name: elements id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: elements-s-elements-e_id-get
          zip_file: elements-e_id-get.zip       

      - name: elements id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r elements-e_id-put.zip elements/e_id/put/index.js node_modules
      - name: elements id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: elements-s-elements-e_id-put
          zip_file: elements-e_id-put.zip       

      - name: elements id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r elements-e_id-delete.zip elements/e_id/delete/index.js node_modules
      - name: elements id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: elements-s-elements-e_id-delete
          zip_file: elements-e_id-delete.zip                              

      # landscapes
      - name: landscapes get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r landscapes-get.zip landscapes/get/index.js node_modules
      - name: landscapes get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: landscape-s-landscape-get
          zip_file: landscapes-get.zip

      - name: landscapes post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r landscapes-post.zip landscapes/post/index.js node_modules
      - name: landscapes post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: landscape-s-landscape-post
          zip_file: landscapes-post.zip

      - name: landscapes id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r landscapes-l_id-get.zip landscapes/l_id/get/index.js node_modules
      - name: landscapes id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: landscape-s-landscape-l_id-get
          zip_file: landscapes-l_id-get.zip       

      - name: landscapes id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r landscapes-l_id-put.zip landscapes/l_id/put/index.js node_modules
      - name: landscapes id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: landscape-s-landscape-l_id-put
          zip_file: landscapes-l_id-put.zip       

      - name: landscapes id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r landscapes-l_id-delete.zip landscapes/l_id/delete/index.js node_modules
      - name: landscapes id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: landscape-s-landscape-l_id-delete
          zip_file: landscapes-l_id-delete.zip                              

      # personas
      - name: personas get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r personas-get.zip personas/get/index.js node_modules
      - name: personas get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: personas-s-personas-get
          zip_file: personas-get.zip

      - name: personas post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r personas-post.zip personas/post/index.js node_modules
      - name: personas post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: personas-s-personas-post
          zip_file: personas-post.zip

      - name: personas id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r personas-p_id-get.zip personas/p_id/get/index.js node_modules
      - name: personas id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: personas-s-personas-p_id-get
          zip_file: personas-p_id-get.zip       

      - name: personas id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r personas-p_id-put.zip personas/p_id/put/index.js node_modules
      - name: personas id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: personas-s-personas-p_id-put
          zip_file: personas-p_id-put.zip       

      - name: personas id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r personas-p_id-delete.zip personas/p_id/delete/index.js node_modules
      - name: personas id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: personas-s-personas-p_id-delete
          zip_file: personas-p_id-delete.zip                              

      # questions
      - name: questions get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r questions-get.zip questions/get/index.js node_modules
      - name: questions get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: questions-s-questions-get
          zip_file: questions-get.zip

      - name: questions post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r questions-post.zip questions/post/index.js node_modules
      - name: questions post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: questions-s-questions-post
          zip_file: questions-post.zip

      - name: questions id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r questions-q_id-get.zip questions/q_id/get/index.js node_modules
      - name: questions id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: questions-s-questions-q_id-get
          zip_file: questions-q_id-get.zip       

      - name: questions id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r questions-q_id-put.zip questions/q_id/put/index.js node_modules
      - name: questions id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: questions-s-questions-q_id-put
          zip_file: questions-q_id-put.zip       

      - name: questions id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r questions-q_id-delete.zip questions/q_id/delete/index.js node_modules
      - name: questions id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: questions-s-questions-q_id-delete
          zip_file: questions-q_id-delete.zip                              

      # roles
      - name: roles get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r roles-get.zip roles/get/index.js node_modules
      - name: roles get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: roles-s-roles-get
          zip_file: roles-get.zip

      - name: roles post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r roles-post.zip roles/post/index.js node_modules
      - name: roles post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: roles-s-roles-post
          zip_file: roles-post.zip

      - name: roles id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r roles-r_id-get.zip roles/r_id/get/index.js node_modules
      - name: roles id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: roles-s-roles-r_id-get
          zip_file: roles-r_id-get.zip       

      - name: roles id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r roles-r_id-put.zip roles/r_id/put/index.js node_modules
      - name: roles id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: roles-s-roles-r_id-put
          zip_file: roles-r_id-put.zip       

      - name: roles id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r roles-r_id-delete.zip roles/r_id/delete/index.js node_modules
      - name: roles id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: roles-s-roles-r_id-delete
          zip_file: roles-r_id-delete.zip                              

      # rules
      - name: rules get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r rules-get.zip rules/get/index.js node_modules
      - name: rules get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: rules-s-rules-get
          zip_file: rules-get.zip

      - name: rules post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r rules-post.zip rules/post/index.js node_modules
      - name: rules post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: rules-s-rules-post
          zip_file: rules-post.zip

      - name: rules id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r rules-r_id-get.zip rules/r_id/get/index.js node_modules
      - name: rules id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: rules-s-rules-r_id-get
          zip_file: rules-r_id-get.zip       

      - name: rules id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r rules-r_id-put.zip rules/r_id/put/index.js node_modules
      - name: rules id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: rules-s-rules-r_id-put
          zip_file: rules-r_id-put.zip       

      - name: rules id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r rules-r_id-delete.zip rules/r_id/delete/index.js node_modules
      - name: rules id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: rules-s-rules-r_id-delete
          zip_file: rules-r_id-delete.zip                              

      # vocabularies
      - name: vocabularies get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r vocabularies-get.zip vocabularies/get/index.js node_modules
      - name: vocabularies get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: vocabularies-s-vocabularies-get
          zip_file: vocabularies-get.zip

      - name: vocabularies post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r vocabularies-post.zip vocabularies/post/index.js node_modules
      - name: vocabularies post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: vocabularies-s-vocabularies-post
          zip_file: vocabularies-post.zip

      - name: vocabularies id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r vocabularies-v_id-get.zip vocabularies/v_id/get/index.js node_modules
      - name: vocabularies id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: vocabularies-s-vocabularies-v_id-get
          zip_file: vocabularies-v_id-get.zip       

      - name: vocabularies id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r vocabularies-v_id-put.zip vocabularies/v_id/put/index.js node_modules
      - name: vocabularies id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: vocabularies-s-vocabularies-v_id-put
          zip_file: vocabularies-v_id-put.zip       

      - name: vocabularies id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r vocabularies-v_id-delete.zip vocabularies/v_id/delete/index.js node_modules
      - name: vocabularies id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: vocabularies-s-vocabularies-v_id-delete
          zip_file: vocabularies-v_id-delete.zip                              

      # tags
      - name: tags get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r tags-get.zip tags/get/index.js node_modules
      - name: tags get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: tags-s-tags-get
          zip_file: tags-get.zip

      - name: tags post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r tags-post.zip tags/post/index.js node_modules
      - name: tags post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: tags-s-tags-post
          zip_file: tags-post.zip

      - name: tags id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r tags-t_id-get.zip tags/t_id/get/index.js node_modules
      - name: tags id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: tags-s-tags-t_id-get
          zip_file: tags-t_id-get.zip       

      - name: tags id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r tags-t_id-put.zip tags/t_id/put/index.js node_modules
      - name: tags id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: tags-s-tags-t_id-put
          zip_file: tags-t_id-put.zip       

      - name: tags id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r tags-t_id-delete.zip tags/t_id/delete/index.js node_modules
      - name: tags id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: tags-s-tags-t_id-delete
          zip_file: tags-t_id-delete.zip                              

      # links
      - name: links get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r links-get.zip links/get/index.js node_modules
      - name: links get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: links-s-links-get
          zip_file: links-get.zip

      - name: links post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r links-post.zip links/post/index.js node_modules
      - name: links post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: links-s-links-post
          zip_file: links-post.zip

      - name: links id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r links-l_id-get.zip links/l_id/get/index.js node_modules
      - name: links id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: links-s-links-l_id-get
          zip_file: links-l_id-get.zip       

      - name: links id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r links-l_id-put.zip links/l_id/put/index.js node_modules
      - name: links id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: links-s-links-l_id-put
          zip_file: links-l_id-put.zip       

      - name: links id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r links-l_id-delete.zip links/l_id/delete/index.js node_modules
      - name: links id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: links-s-links-l_id-delete
          zip_file: links-l_id-delete.zip                              

      # videos
      - name: videos get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r videos-get.zip videos/get/index.js node_modules
      - name: videos get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: videos-s-videos-get
          zip_file: videos-get.zip

      - name: videos post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r videos-post.zip videos/post/index.js node_modules
      - name: videos post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: videos-s-videos-post
          zip_file: videos-post.zip

      - name: videos id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r videos-v_id-get.zip videos/v_id/get/index.js node_modules
      - name: videos id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: videos-s-videos-v_id-get
          zip_file: videos-v_id-get.zip       

      - name: videos id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r videos-v_id-put.zip videos/v_id/put/index.js node_modules
      - name: videos id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: videos-s-videos-v_id-put
          zip_file: videos-v_id-put.zip       

      - name: videos id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r videos-v_id-delete.zip videos/v_id/delete/index.js node_modules
      - name: videos id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: videos-s-videos-v_id-delete
          zip_file: videos-v_id-delete.zip                              

      # tools
      - name: tools get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r tools-get.zip tools/get/index.js node_modules
      - name: tools get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: tools-s-tools-get
          zip_file: tools-get.zip

      - name: tools post lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r tools-post.zip tools/post/index.js node_modules
      - name: tools post lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: tools-s-tools-post
          zip_file: tools-post.zip

      - name: tools id get lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r tools-t-id-get.zip tools/t_id/get/index.js node_modules
      - name: tools id get lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: tools-s-tools-t_id-get
          zip_file: tools-t-id-get.zip       

      - name: tools id put lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r tools-t-id-put.zip tools/t_id/put/index.js node_modules
      - name: tools id put lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: tools-s-tools-t_id-put
          zip_file: tools-t-id-put.zip       

      - name: tools id delete lambda deploy zip
        uses: kinlane/blueprints@main   
        with:
          args: zip -qq -r tools-t-id-delete.zip tools/t_id/delete/index.js node_modules
      - name: tools id delete lambda deploy
        uses: appleboy/lambda-action@master
        with:
          aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_region: us-east-1
          function_name: tools-s-tools-t_id-delete
          zip_file: tools-t-id-delete.zip                              
